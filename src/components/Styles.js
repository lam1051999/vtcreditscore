import {StyleSheet} from 'react-native';

export const OutlineCircleStyles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 1000,
  },
});
